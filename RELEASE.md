# AutoClicker Plus v1.0.0

首个公开发布版本，面向 Windows 桌面自动点击场景。

## 亮点

- 支持全局快捷键开始 / 停止
- 支持快捷键录制，兼容功能键和组合键
- 支持毫秒级点击间隔
- 支持前台与后台两种点击方式
- 支持后台窗口位置 / 区域两种目标选择
- 支持区域随机点 / 中心点策略
- 已提供打包脚本与图标资源

## 已知限制

- 后台点击依赖 Windows 消息机制
- 某些游戏、DirectX 程序、受保护窗口可能不响应
- 需要在桌面环境下运行

## 打包说明

打包后的可执行文件位于：

```text
dist\AutoClicker.exe
```

## 运行方式

```bash
py main.py
```

或直接运行已打包程序：

```text
dist\AutoClicker.exe
```
